/** @jsxRuntime classic */
/** @jsx jsx */
import { jsx } from "@emotion/react";
import { ContainerWrapper, ContainerHeader } from "assets/styles/Common.styles";

export const Battlefield = ({ version, fieldStatus, onChange }) => {
    return (
        <ContainerWrapper>
            <ContainerHeader>Battlefield</ContainerHeader>
        </ContainerWrapper>
    );
};
